[package]
name = "xdot"
version = "0.1.1"
edition = "2021"
license = "MIT"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[dependencies]
anyhow = "1.0.80"
lexopt = "0.3.0"

[build-dependencies]
anyhow = "1.0.80"
